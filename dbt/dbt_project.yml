name: spotify
version: '1.0.0'
config-version: 2

profile: spotify

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  spotify:
    staging:
      spotify:
        +materialized: view
        +schema: staging_spotify
      statsbomb:
        +materialized: view
        +schema: staging_statsbomb
      crypto:
        +materialized: view
        +schema: staging_crypto
      payments:
        +materialized: view
        +schema: staging_payments
    marts:
      spotify:
        +materialized: table
        +schema: marts
      statsbomb:
        +materialized: table
        +schema: marts
      crypto:
        +materialized: incremental
        +schema: marts
      payments:
        +materialized: incremental
        +schema: marts
  elementary:
    +schema: elementary
    +materialized: table

vars:
  # Bucket size (minutes) for payments_by_minute. 1 = finest/realtime, 5 = coarser
  # + faster dashboard. Run payments_by_minute with --full-refresh after changing.
  payments_bucket_minutes: 1
  elementary:
    database_name: "spotify"
